// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom apex domain served from GitHub Pages (see public/CNAME).
  site: 'https://emotions.dev',
});
